# Coda 企业知识库智能体平台

**后端开发**

**技术栈：** Python、FastAPI、LangGraph、Tool Calling、MCP、Skill、Multi-Agent、PostgreSQL、Redis、Milvus、Neo4j

**项目简介：** 基于 LangGraph 搭建企业知识库智能体平台，管理员可统一管理企业资料、智能体能力和访问范围；用户通过自然语言查询内部知识，也可让 Agent 调用工具、拆分子任务，最终获得带原文依据的回答和可下载的任务产物。

**技术亮点：**

- 设计 **Skill 渐进式加载机制**，首轮仅向模型提供 Skill 名称、用途和文件路径，模型读取对应 `SKILL.md` 后才加载所需的本地工具与 MCP 工具；递归展开技能依赖并拦截循环引用、无权限技能和不可用服务，避免把全部工具描述塞入每轮请求，降低上下文占用与误调用风险。

- 设计 **两级会话记忆压缩机制**，默认在上下文达到 100K Tokens 时触发：先将超长工具结果转存文件并保留摘要与回查路径，仍超限时再总结历史并保留最近 10 条消息，缓解长任务的上下文窗口限制，同时避免关键执行结果丢失。

- 设计 **Multi-Agent 并行协作机制**，将复杂任务拆分为多个子任务，并为每个子 Agent 创建独立会话以隔离上下文；提供启动、进度查询、等待和取消完整生命周期，等待超时后任务仍可在后台继续执行，主 Agent 可并行处理其他工作并按 `run_id` 回收结果。

- 构建 **向量、关键词与图谱三路检索链路**，先用语义向量与 BM25 加权召回自然语言和专业术语，再以实体、关系作为种子执行图排序，并融合文本与图谱结果；精排服务异常时自动回退原始召回分数，解决单纯向量检索难以覆盖跨片段关系的问题。

- 构建 **Agent 质量评测与迭代优化闭环**，将真实 AgentRun 一键沉淀为 Replay 样本，围绕 Prompt、模型和 Skills 创建候选配置，并在隔离会话中分别回放基线与候选版本；逐条保存输出、耗时、评分依据和分差，候选版本只有完成实验后才能审批发布，同时保留旧配置支持一键回滚，避免未经验证的配置直接影响线上 Agent。
